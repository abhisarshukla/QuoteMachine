import React, { FunctionComponent } from "react";

const NavBar: FunctionComponent = (props) => {
  return (
    <header className="header">
      <h1 className="header__text">Quotes</h1>
    </header>
  );
};

export default NavBar;
